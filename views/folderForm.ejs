<form
  method="POST"
  action="<%= locals.currentFolderId ? `/folders/${currentFolderId}/create` : '/folders/create' %>"
>
  <label for="folderName">Name</label>
  <input id="folderName" name="folderName" required />
  <button>Create Folder</button>
</form>
